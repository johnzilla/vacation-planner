# This file marks the services/ directory as a Python package.
# Optional: Import service functions here to expose them at the package level.

# Example (uncomment if desired):
# from .vacation_optimizer import optimize_vacation

# Currently empty; add imports or logic as needed.
