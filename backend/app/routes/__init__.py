# This file marks the routes/ directory as a Python package.
# Optional: Import routers here to expose them at the package level.

# Example (uncomment if desired):
# from .suggestions import router as suggestions_router
# from .time_budgets import router as time_budgets_router
# from .holidays import router as holidays_router

# Currently empty; add imports or logic as needed.# This file marks the routes/ directory as a Python package.
# Optional: Import routers here to expose them at the package level.

# Example (uncomment if desired):
# from .suggestions import router as suggestions_router
# from .time_budgets import router as time_budgets_router
# from .holidays import router as holidays_router

# Currently empty; add imports or logic as needed.
